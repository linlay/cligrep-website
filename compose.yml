services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cligrep-website:latest
    ports:
      - "${FRONTEND_PORT:-11801}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
